spring:
  profiles:
    active: dev
  application:
    name: view-link-cloud-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        shared-configs:
          - ${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
    gateway:
      routes:
        - id: web
          uri: lb://view-link-cloud-web
          predicates:
            - Path=/web/**
          filters:
            - StripPrefix=1

        - id: admin
          uri: lb://view-link-cloud-admin
          predicates:
            - Path=/admin/**
          filters:
            - StripPrefix=1
            - CloudAdminFilter

        - id: resource
          uri: lb://view-link-cloud-resource
          predicates:
            - Path=/file/**
          filters:
            - StripPrefix=1
            - CloudAdminFilter

        - id: interact
          uri: lb://view-link-cloud-interact
          predicates:
            - Path=/interact/**
          filters:
            - StripPrefix=1
            - CloudAdminFilter